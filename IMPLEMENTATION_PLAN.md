# 片付け習慣化アプリ 実装計画

最終更新：2026年8月13日（着手トリガーをタイマー形式からストップウォッチ形式へ変更）

## 1. 文書の目的

本書は、片付けの最初の一歩を踏み出しやすくし、毎日の小さな片付けを記録・可視化することで習慣化を支援するアプリの実装計画を定義する。

初期リリースは、iPhoneおよびGoogle PixelのWebブラウザで利用でき、ホーム画面へインストール可能なPWA（Progressive Web App）として開発する。GitHubでソースコードを管理し、GitHub Pagesで公開する。

---

## 2. プロダクトの目的

### 2.1 解決したい課題

- 片付ける箇所が多いため、どこから手をつけるか決められない。
- 一度に大きく片付けようとして、着手の心理的ハードルが高くなる。
- 少し片付けても成果を実感しにくく、継続する動機が弱くなる。
- 片付けなかった日があると、それまでの継続が無駄に感じられる。

### 2.2 提供する価値

- 片付けの内容を簡単に記録し、その日を「片付けできた日」として残せる。
- カレンダー、連続日数、累計日数、バッジにより、小さな前進を可視化できる。
- 片付け場所と内容を自由に追加・編集でき、自分の生活に合わせられる。
- 7日間の継続で得られるリカバリー権により、1日の中断で習慣全体を諦めにくくする。

### 2.3 設計原則

1. **1回の記録を短くする**：通常の記録は3操作程度で完了できるようにする。
2. **量を評価しない**：1個だけ、1か所だけでも、その日の片付けとして等しく扱う。
3. **失敗を強調しない**：未記録日は強い警告色で責める表示にしない。
4. **記録と継続を日付単位で扱う**：同日に何件記録しても連続日数は1日だけ増える。
5. **ゲーム要素を過度に難しくしない**：初回から獲得できるバッジを用意し、次の目標を手が届く範囲に置く。
6. **ストップウォッチは着手のきっかけに限定する**：時間目標や長時間の作業を求めず、「まず開始する」ための任意機能とする。

---

## 3. 初期リリースの範囲

### 3.1 MVPに含める機能

- 今日の片付け記録
- 同じ日の複数記録
- 0秒から加算するストップウォッチ
- ストップウォッチ停止後の記録導線
- 片付け場所の追加、編集、並べ替え、アーカイブ
- 片付け内容の追加、編集、並べ替え、アーカイブ
- 記録ごとの任意メモ
- 今日の記録済み状態
- 現在の連続日数
- 最長連続日数
- 累計片付け日数
- 月間カレンダーによる記録の可視化
- 7日連続達成時のリカバリー権付与
- 過去の未記録日に対するリカバリー権の使用
- バッジの獲得と一覧表示
- オフライン利用
- iPhone、Google Pixelのホーム画面へのインストール
- 端末内データ保存
- JSON形式のバックアップと復元

### 3.2 MVPに含めない機能

- カウントダウンタイマー、時間目標、時間ランキング
- 片付け時間を条件とするバッジや連続記録判定
- 写真の保存
- 家族間の共有、ランキング
- 複数端末間のリアルタイム同期
- AIによる片付け内容の提案
- 自治体別のごみ分別案内
- App Store、Google Playへの公開

---

## 4. 主要なユーザーフロー

### 4.1 初回起動

1. アプリの目的を短く表示する。
2. 初期登録済みの片付け場所・内容を確認する。
3. 必要であれば追加または編集する。設定を変更せず、そのまま開始することもできる。
4. ホーム画面を表示する。

初期候補は次のとおりとする。

- 場所：リビング、キッチン、寝室、玄関、洗面所、浴室、クローゼット、机、その他
- 内容：捨てた、元の場所に戻した、分類した、収納した、拭いた・掃除した、まとめた、その他

### 4.2 今日の片付けを記録する

1. ホーム画面の「今日の片付けを記録」ボタンを押す。
2. 片付け場所を選ぶ。
3. 片付け内容を選ぶ。
4. 必要であればメモを入力する。
5. 「記録する」を押す。
6. 初回記録なら、その日を片付け済みにして連続日数などを更新する。
7. 獲得条件を満たした場合は、リカバリー権またはバッジの獲得を表示する。

場所または内容が既存候補にない場合は、記録画面から新しい項目を追加できるようにする。

### 4.3 ストップウォッチを使って片付けを始める

1. ホーム画面の「まず少しだけ始める」を押す。
2. 「スタート」を押すと0秒から計測を開始し、「少しだけでも十分です」という短いメッセージを表示する。
3. ユーザーが任意の時点で「片付けを終える」を押す。
4. 計測を停止し、「片付けを記録する」を表示して、場所・内容・任意メモの入力へ進む。

補足仕様：

- ストップウォッチの利用は任意とし、従来どおり計測せず直接記録できる。
- 最短時間や達成すべき時間は設定しない。開始直後に終了しても、片付け記録へ進める。
- ストップウォッチを開始しただけでは、その日の片付け済みとして扱わない。「記録する」が完了した時点でカウントする。
- 経過時間に応じた延長要求、警告、評価、称号は表示しない。
- 実測時間は記録の補足情報として保存できるが、ホーム画面の主要指標、連続日数、リカバリー権、バッジ条件には使用しない。
- ストップウォッチを破棄して記録しなかった場合も、失敗表示やペナルティは出さない。

### 4.4 同じ日に追加記録する

- その日の2件目以降の記録も履歴へ保存する。
- 連続日数、累計片付け日数、7日連続の判定は増やさない。
- 場所別・内容別の記録回数には加算する。
- 7日目に何件追加しても、同じ達成日にリカバリー権を再付与しない。

### 4.5 リカバリー権を使う

1. 過去の未記録日が原因で連続記録が途切れる場合、ホームまたはカレンダーに「リカバリーする」を表示する。
2. 対象日と保有数を確認する。
3. 利用を確定する。
4. リカバリー権を1つ消費し、対象日を「リカバリー済み」とする。
5. 連続日数を再計算する。

リカバリーの使用は自動ではなく、ユーザーが明示的に実行する。

---

## 5. 連続日数の仕様

### 5.1 日付の定義

- 判定単位はカレンダー上の1日とする。
- 初期設定のタイムゾーンは `Asia/Tokyo` とする。
- 記録時にはUTC時刻に加えて、判定に使用したローカル日付 `YYYY-MM-DD` を保存する。
- 端末時刻が日付をまたいだ場合、次の記録から新しい日として扱う。

### 5.2 片付け済み日の定義

- その日に有効な片付け記録が1件以上存在する日を「実片付け日」とする。
- 同日に複数件記録しても、実片付け日は1日として数える。
- リカバリー権を使用した日は「保護日」とし、実片付け日とは分けて保存・表示する。
- 実片付け日または保護日が連続していれば、画面上の連続日数は維持される。

### 5.3 今日が未記録の場合

- 今日がまだ終わっていない間は、連続記録を即座に0へ戻さない。
- 「現在の連続日数」と「今日はまだ記録されていません」を併記する。
- 前日以前に未保護の空白日があれば、その時点で連続が途切れたものとして計算する。

### 5.4 過去日の記録

- 通常の片付け記録は当日のみ作成できる。
- 過去日に対して、後から通常記録を追加することはできない。
- 過去の空白日は、保有するリカバリー権を使用した場合のみ保護できる。

この制限により、日付の後付け変更による連続記録やバッジ判定の不整合を防ぐ。

---

## 6. リカバリー権の仕様

### 6.1 付与条件

- 7日連続で**実際の片付け記録**があると、リカバリー権を1つ付与する。
- 14日、21日など、実片付け日の連続日数が7の倍数に到達するたびに1つ付与する。
- リカバリーで保護した日は、画面上の連続日数を維持するが、次のリカバリー権獲得に必要な「実片付け日の連続日数」には含めない。
- リカバリー使用後は、次に7日連続で実際の片付けを記録した時点で新しい権利を付与する。
- 保有数の上限はMVPでは設けない。

### 6.2 重複付与を防ぐ実装

リカバリー権の残数だけを直接加算する実装にはしない。付与履歴を独立したデータとして保存する。

- 実片付け日は `cleanup_days.local_date` を一意キーにする。
- 付与履歴は `recovery_grants.source_date` を一意キーにする。
- 7日目の最初の記録でのみ `cleanup_days` が新規作成される。
- 条件達成時は、付与履歴が存在しない場合に限り、同一トランザクション内で付与履歴を追加する。
- 同じ日の2件目以降の記録、メモ編集、画面再読み込み、アプリ再起動では付与処理を再実行しない。
- UI上の残数は `付与数 - 使用数` から算出し、残数だけを信頼できる唯一のデータとして保存しない。

付与イベントの例：

```text
id: grant-uuid
type: seven_actual_days
sourceDate: 2026-08-19
createdAt: 2026-08-19T12:34:56Z
```

### 6.3 使用条件

- 過去の未記録日1日につき、リカバリー権を1つ使用する。
- 今日および未来の日には使用できない。
- 実片付け日には使用できない。
- 同じ日には1回しか使用できない。
- 保有数が0の場合は使用できない。
- 使用処理は、使用履歴の作成と残数判定を同一トランザクション内で行う。

### 6.4 表示

- 実片付け日：緑色のチェック
- リカバリー日：青色の盾
- 未記録日：淡いグレー
- 今日：輪郭線で強調

色だけで区別せず、アイコンとテキストも併用する。

---

## 7. バッジ仕様

### 7.1 基本方針

- 最初の記録で必ず1つ獲得できるようにする。
- 次の未獲得バッジを1～2個だけホーム画面に表示する。
- 高難度の条件を最初から強調しない。
- 日数系の条件は記録件数ではなく、実片付け日の数で判定する。
- 同じバッジは1回だけ付与し、獲得履歴を保存する。
- リカバリー日は、実際の片付けを条件とするバッジには加算しない。

### 7.2 バッジ一覧

| ID | バッジ名 | 条件 | 意図 |
|---|---|---|---|
| `first_step` | はじめの一歩 | 初めて片付けを記録 | 初日に必ず達成できる |
| `two_days` | ふたつの足あと | 実片付けを2日連続で記録 | 翌日も続ける動機を作る |
| `three_records` | 三つのきらり | 累計記録件数3件 | 同日の小さな行動も評価する |
| `three_days` | 小さな習慣 | 実片付けを3日連続で記録 | 短期目標 |
| `two_places` | ふた部屋さんぽ | 2種類の場所で記録 | 行動範囲を少し広げる |
| `five_days_total` | こつこつ5日 | 累計実片付け日数5日 | 連続が切れても進める目標 |
| `five_day_streak` | 五日咲き | 実片付けを5日連続で記録 | 3日から7日への中間目標 |
| `seven_records` | 7つの前進 | 累計記録件数7件 | 同日複数回の小さな行動も評価する |
| `seven_days` | 一週間達成 | 実片付けを7日連続で記録 | リカバリー権獲得と合わせた節目 |
| `ten_days_total` | 片付けの芽 | 累計実片付け日数10日 | 累計型の継続目標 |
| `fourteen_days` | 二週間のリズム | 実片付けを14日連続で記録 | 一週間の次の習慣化目標 |
| `fifteen_days_total` | 半月の足あと | 累計実片付け日数15日 | 連続が切れても続く中期目標 |
| `twenty_days_total` | 続いている実感 | 累計実片付け日数20日 | 中期目標 |
| `twenty_one_days` | 習慣のつぼみ | 実片付けを21日連続で記録 | 3週間の習慣化を称える |
| `thirty_days_total` | 30日の宝箱 | 累計実片付け日数30日 | 無理なく積み重ねた一か月 |
| `thirty_days` | 一か月の王冠 | 実片付けを30日連続で記録 | 最長の連続目標 |
| `three_places` | 3か所すっきり | 3種類の場所で記録 | 一部分への偏りを責めず、行動範囲を広げる |
| `five_places` | 家の探検家 | 5種類の場所で記録 | 場所の広がりを可視化 |
| `comeback` | 虹の架け橋 | 最初の記録から7日後にも記録 | 途切れを前提にせず、一週間先まで続けたことを称える |
| `recovery_return` | 上手に立て直した | リカバリー使用後の翌日に実片付けを記録 | リカバリー後の再開を支援する |

### 7.3 バッジ判定方法

- 記録作成、リカバリー使用など、条件に関係するイベント後に判定する。
- `badge_awards.badge_id` を一意キーにし、重複付与を防ぐ。
- バッジ定義と判定ロジックを分離し、将来条件を追加しやすくする。
- 獲得条件を満たしたバッジは、保存成功後に1回だけ獲得演出を表示する。

---

## 8. 画面設計

### 8.1 ホーム

表示要素：

- 今日の日付
- 今日の状態：「まだ」または「片付けできました」
- 「今日の片付けを記録」ボタン
- 「まず少しだけ始める」ストップウォッチボタン
- 現在の連続日数
- 今月の片付け日数
- リカバリー権の保有数
- 直近7日間の簡易表示
- 次に獲得できそうなバッジ
- 必要な場合のみ「昨日をリカバリーする」案内

最重要操作である記録ボタンを、片手操作しやすい画面下部付近に大きく表示する。

### 8.2 ストップウォッチ

- 「スタート」ボタン
- 0秒から増える大きな経過時間表示
- 一時停止、再開
- 「片付けを終える」ボタン
- 停止後の「片付けを記録する」ボタン
- 記録せずホームへ戻る操作

画面を閉じたり別アプリへ移動したりしても、開始日時、現在日時、一時停止時間を基準に経過時間を復元する。バックグラウンドでJavaScriptの更新が止まる可能性を考慮し、`setInterval` の呼び出し回数だけで経過時間を算出しない。

### 8.3 記録

- 場所選択
- 内容選択
- メモ入力（任意、複数行）
- ストップウォッチを利用した場合の実測時間（自動入力・編集不可、補助表示）
- 場所・内容のクイック追加
- 保存ボタン

同日に記録済みの場合は、「今日も記録済みです。追加の片付けも残せます」と表示する。

### 8.4 カレンダー・履歴

- 月間カレンダー
- 実片付け日、リカバリー日、未記録日の区別
- 日付を選択した際の記録一覧
- 場所、内容、メモ
- ストップウォッチを利用した記録のみ実測時間を補助表示
- 当日記録の編集・削除（報酬履歴を守るため、その日の最後の1件は削除不可）

### 8.5 バッジ

- 獲得済みバッジ
- 未獲得バッジ
- 条件と現在の進捗
- 獲得日

### 8.6 場所・内容の管理

- 追加
- 名前の編集
- 表示順の変更
- アーカイブ
- アーカイブ解除

過去の記録から参照されている場所・内容は物理削除せず、アーカイブする。履歴では記録当時の名称を保持する。

### 8.7 設定

- タイムゾーン
- データの書き出し
- データの読み込み
- 初期候補の再追加
- アプリ情報
- 全データ削除

全データ削除は破壊的操作であるため、確認画面と確認文の入力を必要とする。

---

## 9. データモデル

### 9.1 `places`：片付け場所

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | UUID | 主キー |
| `name` | string | 表示名 |
| `sortOrder` | number | 表示順 |
| `isArchived` | boolean | アーカイブ状態 |
| `createdAt` | datetime | 作成日時 |
| `updatedAt` | datetime | 更新日時 |

### 9.2 `activities`：片付け内容

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | UUID | 主キー |
| `name` | string | 表示名 |
| `sortOrder` | number | 表示順 |
| `isArchived` | boolean | アーカイブ状態 |
| `createdAt` | datetime | 作成日時 |
| `updatedAt` | datetime | 更新日時 |

### 9.3 `cleanup_records`：個々の記録

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | UUID | 主キー |
| `localDate` | date | 日次判定用の日付 |
| `recordedAt` | datetime | UTCの記録日時 |
| `placeId` | UUID | 場所ID |
| `placeNameSnapshot` | string | 記録時の場所名 |
| `activityId` | UUID | 内容ID |
| `activityNameSnapshot` | string | 記録時の内容名 |
| `memo` | string/null | 任意メモ |
| `stopwatchSessionId` | UUID/null | ストップウォッチを利用した場合のセッションID |
| `elapsedSecondsSnapshot` | number/null | 記録時点の実測秒数 |
| `createdAt` | datetime | 作成日時 |
| `updatedAt` | datetime | 更新日時 |

### 9.4 `stopwatch_sessions`：ストップウォッチ

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | UUID | 主キー |
| `startedAt` | datetime | UTCの開始日時 |
| `pausedAt` | datetime/null | 一時停止日時 |
| `totalPausedSeconds` | number | 累計停止秒数 |
| `endedAt` | datetime/null | 計測終了日時 |
| `elapsedSeconds` | number/null | 実測秒数 |
| `status` | enum | `running`、`paused`、`stopped`、`discarded`、`recorded` |
| `cleanupRecordId` | UUID/null | 作成された片付け記録 |
| `createdAt` | datetime | 作成日時 |
| `updatedAt` | datetime | 更新日時 |

- 同時に実行できるストップウォッチは1件だけとする。
- 実測時間は開始・終了時刻と停止時間から算出し、画面更新回数には依存しない。
- ストップウォッチの停止だけでは `cleanup_days` を作成しない。片付け記録の保存時にのみ日次記録へ反映する。
- 中断・破棄したセッションは履歴集計やバッジに使用しない。

### 9.5 `cleanup_days`：実片付け日

| 項目 | 型 | 説明 |
|---|---|---|
| `localDate` | date | 一意キー |
| `firstRecordId` | UUID | その日の最初の記録 |
| `createdAt` | datetime | 作成日時 |

記録件数とは別に、日次判定の一意性を保証するために使用する。

### 9.6 `recovery_grants`：リカバリー付与履歴

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | UUID | 主キー |
| `sourceDate` | date | 付与条件を満たした日。一意キー |
| `type` | enum | `seven_actual_days` |
| `createdAt` | datetime | 付与日時 |

### 9.7 `recovery_uses`：リカバリー使用履歴

| 項目 | 型 | 説明 |
|---|---|---|
| `id` | UUID | 主キー |
| `targetDate` | date | 保護対象日。一意キー |
| `usedAt` | datetime | 使用日時 |

### 9.8 `badge_awards`：バッジ獲得履歴

| 項目 | 型 | 説明 |
|---|---|---|
| `badgeId` | string | 一意キー |
| `awardedAt` | datetime | 獲得日時 |
| `sourceDate` | date/null | 獲得の基準日 |

### 9.9 `settings`：設定

- データスキーマバージョン
- タイムゾーン
- 初回起動完了状態
- その他の表示設定

---

## 10. 技術構成

### 10.1 フロントエンド

- React
- TypeScript
- Vite
- 状態で切り替える軽量な単一ページ構成
- 通常のCSSとCSS Custom Properties
- 純粋関数による保存データ・インポートデータ検証

### 10.2 データ保存

- IndexedDB
- ブラウザ標準のIndexedDB APIを使用し、アプリ状態を1つのスナップショットとして原子的に保存
- IndexedDBが使用できない場合はlocalStorageへフォールバックし、新旧データの逆転を防ぐマーカーを保存
- 実行中のストップウォッチ状態もIndexedDBへ保存し、画面再表示時に復元
- JSONバックアップにはスキーマバージョンを含める

### 10.3 PWA

- Web App Manifest
- Service Worker
- アプリ本体のオフラインキャッシュ
- iPhone用アイコンとメタデータ
- Pixel/Chrome用インストール導線
- ナビゲーションはnetwork-first、静的アセットはcache-firstで更新とオフライン起動を両立
- ストップウォッチは端末のバックグラウンド制限を考慮し、絶対時刻の差分から表示を補正

PWAではバックグラウンド中にJavaScriptの画面更新が停止する場合があるため、復帰時に保存済みの開始日時と停止時間から経過時間を再計算する。通知権限を前提にせず、ストップウォッチ自体はオフラインで動作させる。

### 10.4 テスト・品質管理

- Node test runner：日付、ストップウォッチ、連続日数、リカバリー、バッジ、バックアップ検証の単体テスト
- Node test runner：ビルド成果物、Manifest、Service WorkerのPWAテスト
- ブラウザ手動検証：320px/390px幅、主要記録フロー、再読み込み、オフライン記録
- TypeScript、ESLint：型・静的検査
- GitHub Actions：テスト、ビルド、GitHub Pagesへのデプロイ

### 10.5 推奨ディレクトリ構成

```text
app/
  CleanupApp.tsx
  globals.css
  lib/
    domain.ts
    storage.ts
    types.ts
src/
  main.tsx
public/
  manifest.webmanifest
  sw.js
  icon-*.png
tests/
  domain.test.ts
  pwa.test.mjs
.github/workflows/
  deploy-pages.yml
```

連続日数、リカバリー、バッジのロジックは画面コンポーネントから分離し、純粋関数としてテストできるようにする。

---

## 11. 実装フェーズ

### フェーズ0：開発基盤

- Vite、React、TypeScriptの初期化
- TypeScript、ESLint、Node test runnerの設定
- GitHub Pages用のビルド設定
- モバイルファーストの基本レイアウト作成

完了条件：ローカルで起動し、テストと本番ビルドが成功する。

### フェーズ1：データ基盤とマスタ管理

- IndexedDBスキーマ
- 初期場所・内容データ
- 場所・内容の追加、編集、並べ替え、アーカイブ
- データアクセス層

完了条件：再起動後も変更内容が保存され、過去記録がマスタ編集の影響を受けない。

### フェーズ2：日々の記録

- ホーム画面
- 記録作成画面
- 同日複数記録
- メモ
- 当日記録の編集・削除（その日の最後の1件は編集のみ）
- 今日の記録済み表示
- ストップウォッチと、停止後の記録導線
- ストップウォッチ状態の保存・復元

完了条件：直接なら最短3操作程度で記録できる。ストップウォッチ経由でも停止後すぐ記録へ進め、同日に複数記録しても片付け日数は1日だけ増える。

### フェーズ3：可視化と連続日数

- 月間カレンダー
- 日別履歴
- 現在・最長連続日数
- 累計・月間片付け日数
- 今日が未記録の場合の猶予表示

完了条件：日付境界と空白日を含む連続日数が仕様どおり計算される。

### フェーズ4：リカバリー権

- 7日連続の判定
- 付与履歴
- 保有数表示
- 未記録日への使用
- カレンダーの保護日表示
- 重複付与防止

完了条件：7日目に何件記録・編集・再読み込みしても付与は1回だけで、14日目には合計2回付与される。

### フェーズ5：バッジ

- バッジ定義
- 判定エンジン
- 獲得演出
- 一覧と進捗
- 重複付与防止

完了条件：全バッジの境界条件と一意性テストが成功する。

### フェーズ6：PWAとバックアップ

- Manifest、アイコン、Service Worker
- オフライン起動
- インストール案内
- JSON書き出し・読み込み
- データ形式の検証と移行

完了条件：iPhoneとPixelでホーム画面から起動でき、オフラインで記録・閲覧できる。

### フェーズ7：総合検証と公開

- 実機検証
- アクセシビリティ確認
- 主要ブラウザ確認
- エラー時表示
- GitHub Actionsによる自動デプロイ
- READMEと利用手順の作成

完了条件：受け入れ基準をすべて満たし、GitHub Pagesの公開URLから利用できる。

---

## 12. 必須テストケース

### 12.1 日次記録

1. 初回記録で累計日数が1になる。
2. 同日に2件記録しても累計日数と連続日数は増えない。
3. 翌日の初回記録で連続日数が1増える。
4. 同日記録のメモ編集で日数が増えない。
5. 記録時刻が23:59と翌日00:00の場合、別日として扱われる。

### 12.2 ストップウォッチ

1. 「スタート」で0秒から計測を開始できる。
2. 予定時間や終了時刻を選択する操作が存在しない。
3. ストップウォッチを開始しただけでは、その日の片付け記録や連続日数は増えない。
4. 「片付けを終える」で計測を停止し、記録画面へ進める。
5. 記録を保存すると、その日の片付けとしてカウントされ、実測時間が関連付けられる。
6. 一時停止中の時間は実測時間に含まれない。
7. 画面をバックグラウンドへ移してから戻ると、実際に経過した時間へ表示が補正される。
8. ページ再読み込み後も実行中または一時停止中の状態を復元できる。
9. ストップウォッチを破棄した場合、片付け日数、連続日数、バッジに影響しない。
10. ストップウォッチ利用記録と直接作成した記録のどちらも、同じ日次判定規則に従う。

### 12.3 リカバリー権の重複防止

1. 6日連続では付与されない。
2. 7日目の最初の記録で1つ付与される。
3. 7日目の2件目、3件目の記録では追加付与されない。
4. 7日目の記録を編集しても追加付与されない。
5. 7日目に画面を再読み込み・アプリを再起動しても追加付与されない。
6. 同一処理が重複実行されても `sourceDate` の一意制約で1件だけ保存される。
7. 14日連続の到達時には新たに1つだけ付与され、累計2つになる。
8. リカバリー日を含むだけでは、新しい権利は付与されない。

### 12.4 リカバリー権の使用

1. 保有数0では使用できない。
2. 1日を保護すると保有数が1減り、連続日数が維持される。
3. 同じ日への2回目の使用はできない。
4. 実片付け日、今日、未来の日には使用できない。
5. 2日連続の空白を守るには2つ必要になる。
6. リカバリー使用後に7日連続で実際に記録すると、新しい権利が1つ付与される。

### 12.5 バッジ

1. 初回記録で「はじめの一歩」を獲得する。
2. 同じバッジは再読み込みや再判定で重複獲得しない。
3. 累計日数系は同日複数記録で増えない。
4. 記録件数系は同日複数記録を正しく数える。
5. アーカイブ済みの場所も、過去の場所種類数に含まれる。
6. リカバリー日は実片付け日条件に含まれない。

### 12.6 バックアップ

1. 書き出したJSONを初期状態へ正しく復元できる。
2. 不正なファイルを読み込んでも既存データを破損しない。
3. 古いスキーマバージョンを移行できる。
4. 付与履歴とバッジ履歴も復元され、重複付与されない。

---

## 13. 受け入れ基準

- iPhoneおよびGoogle Pixelの縦画面で、横スクロールなしに利用できる。
- ホーム画面から片付け記録を短い操作で完了できる。
- ストップウォッチを使わない直接記録と、ストップウォッチから始める方法の両方を選べる。
- ストップウォッチは0秒から加算し、予定時間や終了時刻を要求しない。
- 任意の時点で停止し、片付け記録へ進める。
- ストップウォッチ開始だけでは片付け済みにならず、記録保存時に初めてその日の活動へカウントされる。
- 実測時間は補足情報にとどめ、連続日数、リカバリー権、バッジの条件に影響しない。
- 場所・内容をアプリ内で追加・編集できる。
- 記録ごとに任意のメモを残せる。
- 1日に複数件記録しても、連続日数は1日分だけ増える。
- 実片付けを7日連続で記録すると、リカバリー権が1つ付与される。
- 7日目に複数回記録しても、リカバリー権は1回しか付与されない。
- リカバリー権を使用すると、対象の未記録日を含めて画面上の連続日数が維持される。
- 実片付け日とリカバリー日を視覚的かつ意味的に区別できる。
- 簡単な条件から段階的にバッジを獲得できる。
- オフライン状態でも記録と履歴閲覧ができる。
- 端末内データをバックアップ・復元できる。

---

## 14. リスクと対策

| リスク | 対策 |
|---|---|
| 連続日数がプレッシャーになる | 未記録を強い警告色にしない。累計日数や再開バッジも併記する |
| 7日目の操作で権利が重複付与される | 日付単位の一意キー、付与履歴、トランザクション、重複テストを導入する |
| マスタ名の変更で過去履歴が変わる | 記録時の名称をスナップショットとして保存する |
| 端末のブラウザデータ削除で記録を失う | バックアップ機能と最終バックアップ日の案内を用意する |
| 日付変更やタイムゾーンで連続判定がずれる | 判定用ローカル日付とUTC時刻を両方保存し、日付境界をテストする |
| バッジ条件が遠すぎて意欲を失う | 1、2、3、5、7、10、20日の近い節目を中心にする |
| 入力項目が多く記録自体が負担になる | 場所・内容は大きな選択ボタン、メモは任意、直近選択を上位表示する |
| ストップウォッチの長い記録が競争や圧力になる | 時間目標・ランキング・時間バッジを設けず、実測時間を補助情報としてのみ表示する |
| PWAがバックグラウンドで画面更新を停止する | 開始日時、現在日時、一時停止時間から経過時間を再計算し、復帰時に表示を補正する |

---

## 15. 将来拡張

MVPの利用感を確認した後、必要性が高い順に検討する。

1. Supabase等を利用したログインと複数端末同期
2. やさしいリマインダー通知
3. ホーム画面ウィジェット相当の表示
4. 写真によるビフォー・アフター記録
5. 場所ごとの最近の活動や偏りの可視化
6. ユーザーがバッジ条件を追加できる機能
7. Capacitor等によるApp Store・Google Play向けパッケージ化

通知を実装する場合も、「未達成」という表現ではなく「今日は小さな一歩を記録できます」のような任意・肯定的な文面を使用する。

---

## 16. 実装開始時の最初の作業

1. プロジェクト雛形とテスト環境を作成する。
2. `cleanup_days`、`recovery_grants`、`recovery_uses`を含むデータスキーマを先に実装する。
3. 画面実装より前に、連続日数・リカバリー権の必須テストを作成する。
4. 記録フローとホーム画面を実装する。
5. 実機で「記録までの操作数」と片手操作性を確認する。

リカバリー権の重複付与はプロダクトの信頼性に直結するため、UIより先にドメインロジックと永続化の一意性を確定させる。
